---
description: Search XContext for preserved evidence.
argument-hint: <query>
---

Call `xcontext_search` with `$ARGUMENTS`. Return matching `ctx://` references and short summaries. Retrieve full redacted content only when the user asks or the evidence is required for the current task.
