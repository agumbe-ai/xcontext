---
description: Check the active XContext mode, MCP connection, storage, and usage counters
---

Call `mcp__xcontext__xcontext_stats`. Report whether XContext is connected and summarize sessions, objects, token savings, redactions, and retrievals. If the tool is unavailable, explain how to run `agumbe-ctl xcontext status` and inspect `/mcp`.
